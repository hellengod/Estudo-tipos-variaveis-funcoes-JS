/* Declare duas variáveis, uma contendo um número e outra contendo uma string. 
Em seguida, combine-as em uma terceira variável usando template strings para montar uma frase e exiba 
o resultado no console. */
const nome = "Hellen";
const idade = "20";

const template = `Meu nome e ${nome} e minha idade e ${idade}`;
console.log(template);